function Itinerary(ports) {
  this.ports = ports;      // Itinerary should take an array of ports, instead of 2 variables - this way the itinerary can
}                       // be extended to however many ports you want to add to it


module.exports = Itinerary;
